{
  "name": "zombie-farm",
  "version": "1.0.0",
  "description": "A reverse Plants vs Zombies game - command the zombie horde!",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run sync-assets && vite build",
    "sync-assets": "node scripts/sync-assets.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}

